{
  "name": "openai-automated-ts",
  "description": "Automatic tracing of openai calls in a TypeScript application",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "start": "npm run build && dotenv -e .env -- node dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@autoblocks/client": "^0.0.30",
    "@types/node": "^20.8.9",
    "dotenv-cli": "^7.3.0",
    "openai": "^4.6.0",
    "typescript": "^5.2.2"
  }
}
