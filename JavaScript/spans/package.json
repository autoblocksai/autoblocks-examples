{
  "name": "spans",
  "description": "Establish parent / child relationships between your events with the `spanId` and `parentSpanId` properties",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "dotenv -e .env -- node ./src/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@autoblocks/client": "^0.0.62",
    "dotenv-cli": "^7.3.0"
  }
}
